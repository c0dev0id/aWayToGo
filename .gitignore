.gradle/
*.iml
.idea/
local.properties
/build/
app/build/
rust/target/

# local credentials / helper scripts — never commit these
.gh_token
doit.sh

# device log dumps (make log)
log/

# Claude Code local state (not shared)
.claude/settings.local.json
.claude/worktrees/
